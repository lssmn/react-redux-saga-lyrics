import React from 'react';
import styles from './header.css';

export const Header = () => (
  <header className={styles.header}>
    <div className={styles.logo}>react-redux-sagas-lyrics</div>
  </header>
);

export default Header;
